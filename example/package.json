{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "rules": "cursor-rules",
    "repomix": "cursor-rules --repomix",
    "yolo": "cursor-rules --force",
    "clean": "rm -rf .cursor repomix-output.xml repomix.config.json"
  },
  "dependencies": {
    "@gabimoncha/cursor-rules": "workspace:*"
  }
}
