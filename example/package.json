{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "rules:dev": "bun run --bun cursor-rules",
    "rules:lib": "cursor-rules",
    "clean": "rm -rf .cursor repomix-output.xml repomix.config.json"
  },
  "devDependencies": {
    "@gabimoncha/cursor-rules": "workspace:*"
  }
}
